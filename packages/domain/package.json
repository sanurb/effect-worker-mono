{
  "name": "@repo/domain",
  "scripts": {
    "build": "tsgo -p tsconfig.build.json",
    "check": "tsgo -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/tsgo": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}
