id: no-runtime-runfork

valid:
  - |
      Effect.gen(function*() {
        const fiber = yield* Effect.forkScoped(backgroundTask);
      });

invalid:
  - |
      const fiber = Runtime.runFork(myEffect);
