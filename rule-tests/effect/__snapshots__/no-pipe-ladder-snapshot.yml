id: no-pipe-ladder
snapshots:
  ? |
    pipe(
      pipe(getUser(id), Effect.map(transform)),
      Effect.flatMap(processUser)
    );
  : labels:
    - source: |-
        pipe(
          pipe(getUser(id), Effect.map(transform)),
          Effect.flatMap(processUser)
        )
      style: primary
      start: 0
      end: 81
