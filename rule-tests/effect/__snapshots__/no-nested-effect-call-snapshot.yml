id: no-nested-effect-call
snapshots:
  ? |
    Effect.andThen(Effect.flatMap(Effect.succeed(x), f1), f2);
  : labels:
      - source: Effect.andThen(Effect.flatMap(Effect.succeed(x), f1), f2)
        style: primary
        start: 0
        end: 57
  ? |
    Effect.flatMap(Effect.map(Effect.succeed(value), fn1), fn2);
  : labels:
      - source: Effect.flatMap(Effect.map(Effect.succeed(value), fn1), fn2)
        style: primary
        start: 0
        end: 59
