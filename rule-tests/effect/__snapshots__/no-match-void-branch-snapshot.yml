id: no-match-void-branch
snapshots:
  ? |
    Match.value(event).pipe(
      Match.when("update", () => handleUpdate(event)),
      Match.orElse(() => Effect.void)
    );
  : labels:
    - source: Match.orElse(() => Effect.void)
      style: primary
      start: 78
      end: 109
  ? |
    Match.when(true, () => Effect.void);
  : labels:
    - source: Match.when(true, () => Effect.void)
      style: primary
      start: 0
      end: 35
