id: no-interface-in-models
snapshots:
  ? |
    export interface User {
      readonly id: string;
      readonly email: string;
    }
  : labels:
      - source: |-
          export interface User {
            readonly id: string;
            readonly email: string;
          }
        style: primary
        start: 0
        end: 74
