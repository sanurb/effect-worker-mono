id: no-effect-bind
snapshots:
  ? |
    pipe(
      Effect.Do,
      Effect.bind("user", () => getUser(id))
    );
  : labels:
    - source: Effect.bind("user", () => getUser(id))
      style: primary
      start: 21
      end: 59
