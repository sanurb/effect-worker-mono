id: no-manual-tag-check
snapshots:
  ? |
    if ("UserNotFoundError" == cause._tag) {
      return Option.none();
    }
  : labels:
      - source: '"UserNotFoundError" == cause._tag'
        style: primary
        start: 4
        end: 37
  ? |
    if (error._tag === "ValidationError") {
      return fallback;
    }
  : labels:
      - source: error._tag === "ValidationError"
        style: primary
        start: 4
        end: 36
