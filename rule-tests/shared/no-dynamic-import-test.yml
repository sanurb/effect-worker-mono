id: no-dynamic-import

valid:
  - |
      import { UserSchema } from "@repo/domain";
      export const schema = UserSchema;
  - |
      const loader = () => Promise.resolve(moduleFactory);

invalid:
  - |
      const module = await import("./module");
  - |
      const page = lazy(() => import("./heavy"));
